<style>
  :root {
    --naranja-principal: #F05523;
    --naranja-claro: #FFE6DD;
    --gris-oscuro: #2C3E50;
    --beige: #FDF6F2;
    --font-primary: 'Playfair Display', serif;
    --font-secondary: 'Lato', sans-serif;
  }
  
  .hero-section {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: var(--beige);
    position: relative;
    overflow: hidden;
    padding: 2rem;
  }
  
  .hero-content {
    max-width: 68rem;
    margin: 0 auto;
    position: relative;
    z-index: 2;
    opacity: 0;
    animation: fadeIn 1s ease-out forwards;
    padding: 4rem 2rem;
    text-align: center;
  }
  
  .subtitle {
    font-family: var(--font-secondary);
    color: var(--naranja-principal);
    font-size: 0.9rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    margin-bottom: 2rem;
    display: block;
    font-weight: 500;
  }
  
  .title {
    font-family: var(--font-primary);
    color: var(--gris-oscuro);
    font-size: clamp(2.2rem, 4vw, 3.2rem);
    line-height: 1.3;
    margin-bottom: 2.5rem;
    position: relative;
    font-weight: 500;
    padding-bottom: 1.5rem;
  }
  
  .title::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 60px;
    height: 1px;
    background: var(--naranja-principal);
  }
  
  .description {
    font-family: var(--font-secondary);
    color: var(--gris-oscuro);
    font-size: clamp(1rem, 2vw, 1.15rem);
    line-height: 1.8;
    margin-bottom: 3rem;
    max-width: 52rem;
    margin-left: auto;
    margin-right: auto;
    font-weight: 400;
  }
  
  .cta-button {
    display: inline-flex;
    align-items: center;
    padding: 1rem 2.8rem;
    background-color: var(--naranja-principal);
    color: white;
    font-family: var(--font-secondary);
    font-weight: 500;
    font-size: 1rem;
    border-radius: 3px;
    position: relative;
    overflow: hidden;
    transition: all 0.3s ease;
    text-decoration: none;
    letter-spacing: 0.5px;
  }
  
  .cta-button:hover {
    transform: translateY(-2px);
    background-color: #e44512;
  }
  
  .decorative-element {
    position: absolute;
    top: -5%;
    right: -5%;
    width: 30rem;
    height: 30rem;
    background: var(--naranja-claro);
    border-radius: 50%;
    opacity: 0.08;
    transform: rotate(-15deg);
  }
  
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  @media (max-width: 768px) {
    .hero-section {
      padding: 1rem;
    }
    
    .hero-content {
      padding: 2rem 1rem;
    }
    
    .decorative-element {
      width: 20rem;
      height: 20rem;
    }
    
    .title {
      padding-bottom: 1rem;
    }
    
    .description {
      font-size: 1rem;
      line-height: 1.6;
    }
    
    .cta-button {
      padding: 0.9rem 2rem;
    }
  }
</style>

<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500&family=Lato:wght@400;500&display=swap" rel="stylesheet">

<section class="hero-section">
  <div class="hero-content">
    <span class="subtitle">ABOGADOS DERECHO DE FAMILIA</span>
    <h1 class="title">Protegiendo lo que más valoras</h1>
    <p class="description">
      Experiencia, preparación y defensa son cruciales para proteger a tu familia en casos de derecho de alimentos, medidas de protección, cuidado personal u otros asuntos de derecho familiar. Ofrecemos asesoramiento confiable incluso en los casos más complejos.
    </p>
    <a href="https://wa.me/56944947427?text=Hola,%20me%20interesa%20una%20consulta%20sobre%20derecho%20familiar" 
       class="cta-button"
       target="_blank"
       rel="noopener noreferrer">
      Consulta Gratuita
    </a>
  </div>
  <div class="decorative-element"></div>
</section>